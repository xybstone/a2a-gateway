name: Notify on Push to Slack

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**.md'
      - '**.txt'
      - 'docs/**'
      - '.gitignore'
      - '.gitattributes'

jobs:
  notify-slack:
    name: Send notification to Slack
    runs-on: ubuntu-latest
    steps:
      - name: Send Slack notification
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
        run: |
          # 准备消息内容
          COMMIT_AUTHOR="${{ github.actor }}"
          COMMIT_SHA="${{ github.sha }}"
          COMMIT_MESSAGE="${{ github.event.head_commit.message }}"
          COMMIT_URL="${{ github.event.head_commit.url }}"
          BRANCH="${{ github.ref_name }}"
          TIMESTAMP=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
          
          # 获取变更的文件
          CHANGED_FILES=$(echo "${{ github.event.head_commit.added }} ${{ github.event.head_commit.modified }}" | tr ' ' '\n' | head -10)
          
          # 发送 Slack 消息（使用单行 JSON，避免 heredoc 问题）
          curl -X POST "${SLACK_WEBHOOK_URL}" \
            -H 'Content-Type: application/json' \
            -d '{"text":"📬 New commit to a2a-gateway\n\n👤 Author: '"'"${COMMIT_AUTHOR}"'"'\n🌿 Branch: '"'"${BRANCH}"'"'\n📝 Commit: '"'"${COMMIT_MESSAGE:0:100}"'"'...\n🔗 URL: '"'"${COMMIT_URL}"'"'\n🕐 Time: '"'"${TIMESTAMP}"'"'\n\n📁 Changed files:\n'"'"${CHANGED_FILES}"'"'\n\n🤖 三弟正在分析这次提交..."}'
          
          echo "Slack notification sent successfully"
ß